// import { Picker, Dialog, Toast, Popup } from 'vant';
import Vue from 'vue';
// import { TOAST_DEFAULT_CONFIG } from '@/const/vantConfig';

export default () => {
  // 设置 Toast
  // Toast.setDefaultOptions(TOAST_DEFAULT_CONFIG);
  // Toast.allowMultiple(true);

  // const depends = [Picker, Dialog, Toast, Popup];
  // depends.forEach(v => {
  //   Vue.use(v);
  // });

  // if (!SVGCircleElement.prototype.getTotalLength) {
  //   SVGCircleElement.prototype.getTotalLength = function(this: any) {
  //     let width = this.parentNode.clientWidth, // Get the parent node width (trying to get the current node width returns 0)
  //       radius = width / 2, // Get the radius by dividing by 2
  //       length = 2 * Math.PI * radius; // Get the circumference from 2πr

  //     // Return the calculated value
  //     return length;
  //   };
  // }
};
